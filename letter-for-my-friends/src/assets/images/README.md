# Images

Optional photos for the letter (shown as a small polaroid), one per
friend. Reference the path via the `photo` field in
`src/data/friends.js`.

Also a good place for a custom envelope texture/illustration if you
want to replace the CSS-drawn envelope in
`src/components/Envelope.jsx`.
