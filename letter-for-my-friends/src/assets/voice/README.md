# Voice Notes

Put each friend's recorded voice message here, e.g. `aarifa.mp3`.

Each friend object in `src/data/friends.js` has a `voiceNote.src`
field pointing to a file in this folder — update it to match your
recording's filename.

Keep clips short (15–60s) and in mp3 or m4a format.
